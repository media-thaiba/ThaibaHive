import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Add your network IP here if accessing from another device:
  // allowedDevOrigins: ["192.168.0.136"],
};

export default nextConfig;
